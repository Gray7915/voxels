{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug voxels",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/voxels.exe",
            "cwd": "${workspaceFolder}/build",
            "stopAtEntry": false,
            "console": "integratedTerminal"
        }
    ]
}